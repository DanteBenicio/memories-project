import { ActionType } from '../types/Action-Types';

export const {
  FETCH_ALL, CREATE, UPDATE, DELETE, LIKE,
}: ActionType = {
  FETCH_ALL: 'FETCH_ALL',
  CREATE: 'CREATE',
  UPDATE: 'UPDATE',
  DELETE: 'DELETE',
  LIKE: 'LIKE',
};
